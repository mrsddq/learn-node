# Learn Node

A Node.js learning archive containing lessons and small Express applications.

## Structure

```text
1Nodejs-TheBeginning/
2Node.js-WritingOurFirstServer/
3MyFirstExpressAppAListOfContacts/
4MyFirstExpressAppContinued/
package.json
```

## Setup

```bash
npm install
npm start
```

`npm start` runs the most complete contact-list lesson under `4MyFirstExpressAppContinued/12DeletingAContact/`.

## Status

Baseline repository structure is complete:

- lesson folders
- package metadata
- generated `node_modules/` removed from Git tracking
- root README
- root `.gitignore`

## Maintenance Note

Keep this repo archived. If a Node project from this archive becomes portfolio-facing, extract it into a clean repo with only source, lockfiles, tests, and docs.

Use [docs/LESSON_INDEX.md](docs/LESSON_INDEX.md) to find the strongest lesson folders before extracting anything.
